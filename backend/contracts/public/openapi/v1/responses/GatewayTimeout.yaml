description: Gateway Timeout response
content:
  application/json:
    schema:
      $ref: "../components/responses/BaseResponse.yaml"
    example:
      ok: false
      message: Can not process operation
